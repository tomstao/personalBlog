---
import { SITE, LINKS } from "@consts"
import { cn } from "@lib/utils"
import Container from "@components/Container.astro"
import IconButton from "@components/IconButton.astro"

const { pathname } = Astro.url
const subpath = pathname.match(/[^/]+/g)
---

<header id="header" class="fixed top-0 z-50 h-16 w-full">
  <Container size="md">
    <div class="relative h-full w-full">
      <div class="absolute left-0 top-1/2 flex -translate-y-1/2 gap-2 font-semibold">
        <a
          href="/"
          class="flex items-center gap-2 rounded-sm text-current outline-none transition-colors duration-300 ease-in-out hover:text-black focus-visible:ring-2 focus-visible:ring-black/50 dark:hover:text-white dark:focus-visible:ring-white/50"
        >
          <img
            src="https://github.com/tomstao.png"
            alt="Tao Su's avatar"
            class="size-8 rounded-full border-2 border-black/10 transition-all duration-300 hover:shadow-[0_0_20px_rgba(249,115,22,0.4)] dark:border-white/25 dark:hover:shadow-[0_0_20px_rgba(251,191,36,0.6)]"
          />
          <div>
            {SITE.TITLE}
          </div>
        </a>
      </div>

      <div class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2">
        <nav class="hidden items-center justify-center gap-1 text-sm md:flex">
          {
            LINKS.map((LINK) => (
              <a
                href={LINK.HREF}
                class={cn(
                  "h-8 rounded-full px-3 text-current",
                  "flex items-center justify-center",
                  "transition-colors duration-300 ease-in-out",
                  "outline-none focus-visible:ring-2 focus-visible:ring-black/50 dark:focus-visible:ring-white/50",
                  pathname === LINK.HREF || "/" + subpath?.[0] === LINK.HREF
                    ? "bg-black text-white dark:bg-white dark:text-black"
                    : "hover:bg-black/5 hover:text-black dark:hover:bg-white/20 dark:hover:text-white"
                )}
              >
                {LINK.TEXT}
              </a>
            ))
          }
        </nav>
      </div>

      <div class="buttons absolute right-0 top-1/2 flex -translate-y-1/2 gap-1">
        <IconButton
          id="search-button"
          ariaLabel={`Search blog posts and projects on ${SITE.TITLE}`}
          icon="search"
          visibility="hidden md:flex"
        />

        <IconButton
          href="/rss.xml"
          external
          ariaLabel={`Rss feed for ${SITE.TITLE}`}
          icon="rss"
          visibility="hidden md:flex"
        />

        <IconButton
          id="header-theme-button"
          ariaLabel="Toggle light and dark theme"
          icon="sun"
          darkIcon="moon"
          visibility="hidden md:flex"
        />

        <button
          id="header-drawer-button"
          aria-label="Toggle drawer open and closed"
          class={cn(
            "flex md:hidden",
            "size-9 rounded-full p-2 items-center justify-center",
            "bg-transparent hover:bg-black/5 dark:hover:bg-white/20",
            "stroke-current hover:stroke-black hover:dark:stroke-white",
            "border border-black/10 dark:border-white/25",
            "transition-colors duration-300 ease-in-out",
            "outline-none focus-visible:ring-2 focus-visible:ring-black/50 dark:focus-visible:ring-white/50"
          )}
        >
          <svg id="drawer-open" class="size-full">
            <use href="/ui.svg#menu"></use>
          </svg>
          <svg id="drawer-close" class="size-full">
            <use href="/ui.svg#x"></use>
          </svg>
        </button>
      </div>
    </div>
  </Container>
</header>

<style>
  #header-drawer-button > #drawer-open {
    @apply block;
  }

  #header-drawer-button > #drawer-close {
    @apply hidden;
  }

  #header-drawer-button.open > #drawer-open {
    @apply hidden;
  }

  #header-drawer-button.open > #drawer-close {
    @apply block;
  }
</style>

<script is:inline>
  function toggleDrawer() {
    const drawer = document.getElementById("drawer")
    const drawerButton = document.getElementById("header-drawer-button")
    drawer?.classList.toggle("open")
    drawerButton?.classList.toggle("open")
  }

  function openSearchModal() {
    window.dispatchEvent(new CustomEvent("open-search-modal"))
  }

  function initializeHeader() {
    const drawerButton = document.getElementById("header-drawer-button")
    drawerButton?.addEventListener("click", toggleDrawer)

    const searchButton = document.getElementById("search-button")
    searchButton?.addEventListener("click", openSearchModal)

    // Use shared nav utility (defined in public/js/nav.js)
    if (typeof updateHeaderNav === "function") {
      updateHeaderNav()
    }
  }

  document.addEventListener("astro:after-swap", initializeHeader)
  initializeHeader()
</script>
