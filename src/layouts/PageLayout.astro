---
import "@styles/global.css"
import BaseHead from "@components/BaseHead.astro"
import Header from "@components/Header.astro"
import Footer from "@components/Footer.astro"
import Drawer from "@components/Drawer.astro"
import BackToTop from "@components/BackToTop.astro"
import ReadingProgress from "@components/ReadingProgress.astro"
import SearchModal from "@components/SearchModal"
import { SITE } from "@consts"
import { type CollectionEntry, getCollection } from "astro:content"

interface Props {
  title: string
  description: string
  article?: {
    publishedTime: Date
    modifiedTime?: Date
    tags?: string[]
  }
}

const { title, description, article } = Astro.props

// Get data for search modal
const posts = (await getCollection("blog")).filter((post) => !post.data.draft)
const projects = (await getCollection("projects")).filter((project) => !project.data.draft)
const searchData = [...posts, ...projects] as CollectionEntry<"blog">[]
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={`${title} | ${SITE.TITLE}`} description={description} article={article} />
  </head>
  <body>
    {article && <ReadingProgress />}
    <Header />
    <Drawer />
    <main>
      <slot />
    </main>
    <Footer />
    <BackToTop />
    <SearchModal client:only="solid-js" data={searchData} />
  </body>
</html>
